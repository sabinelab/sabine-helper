model Blacklist {
  id     String        @id
  type   BlacklistType
  name   String?
  when   DateTime      @default(now())
  reason String
  endsAt DateTime?     @map("ends_at")

  @@index([type])
}

enum BlacklistType {
  USER
  GUILD
}
